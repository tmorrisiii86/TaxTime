@model IEnumerable<TaxTime4.Models.Appointment>

@{
    ViewData["Title"] = "ColdCall";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Customer.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastAppt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NextAppt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contact.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastUpdated)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelitem => item.Customer.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelitem => item.Customer.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastAppt)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NextAppt)
                </td>
                <td>
                    @Html.DisplayNameFor(modelitem => item.Contact.Email)
                </td>
                <th>
                    @Html.DisplayNameFor(modelitem => item.LastUpdated)
                <td>
                    <a asp-action="Edit" asp-route-id="@item.CustId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.CustId">Details</a> 
                    <a asp-action="Delete" asp-route-id="@item.CustId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
